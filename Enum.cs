using System;
using System.Collections.Generic;
using System.Text;

namespace PromotionEngine.Enum
{
    /// <summary>
    /// The IndividualPrices.
    /// </summary>
    public enum IndividualPrices
    {
        A = 50,
        B = 30,
        C = 20,
        D = 15
    }

    public enum RuleQuantity
    {
        A = 3,
        B = 2
    }

    public enum RuleQuantityPrice
    {
        A = 130,
        B = 45,
        CD = 30
    }
}
